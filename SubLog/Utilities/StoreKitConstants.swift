enum StoreKitConstants {
    enum ProductID {
        static let monthly = "jp.sublog.premium.monthly"
        static let annual = "jp.sublog.premium.annual"

        static let all: [String] = [monthly, annual]
    }

    enum SubscriptionGroup {
        static let name = "SubLogPremium"
    }
}
